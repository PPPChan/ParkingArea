var sub = function(val) {
  if (val.length == 0 || val == undefined) {
    return;
  }
  if (val.length > 7) {
    return "..."+val.substring(val.length-7, val.length) ;
  } else {
    return val;
  }
}
module.exports.sub = sub;